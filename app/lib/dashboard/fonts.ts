import { Figtree } from 'next/font/google';

// Import Patrick Hand font
export const PatrickHand = Figtree({
  subsets: ['latin'],
  weight: '400', // Regular weight for Patrick Hand
});

// You can also export any other fonts you have
export const figtree = Figtree({
  subsets: ['latin'],
});

